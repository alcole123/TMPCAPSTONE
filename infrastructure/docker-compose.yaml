version: '3'

services:
  splunk:
    container_name: splunk
    image: splunk/splunk:latest
    volumes:
      - ./splunk:/var/splunk
    restart: always
    env_file:
      - .env
    ports:
      -  8000:443
      -  8088:80
      -  9997:9997
